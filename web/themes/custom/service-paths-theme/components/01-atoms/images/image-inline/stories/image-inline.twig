<div class="sg-images">
  {% for item in items %}
    <div class="sg-images__item">
      <div class="sg-images__image sg-images__image--small" data-image="{{ item.value }}"></div>
      <pre>{{ item.name }}</pre>
    </div>
  {% endfor %}
</div>
